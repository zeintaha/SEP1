/**
 * Sample Skeleton for 'Edit_Activity_Subject.fxml' Controller Class
 */

import java.net.URL;
import java.util.ResourceBundle;
import javafx.fxml.FXML;
import javafx.scene.control.Button;
import javafx.scene.control.ComboBox;
import javafx.scene.control.TextField;

public class PleaseProvideControllerClassName {

    @FXML // ResourceBundle that was given to the FXMLLoader
    private ResourceBundle resources;

    @FXML // URL location of the FXML file that was given to the FXMLLoader
    private URL location;

    @FXML // fx:id="btnSaveChanges"
    private Button btnSaveChanges; // Value injected by FXMLLoader

    @FXML // fx:id="txtActivity"
    private TextField txtActivity; // Value injected by FXMLLoader

    @FXML // fx:id="cmbActivitySubject"
    private ComboBox<?> cmbActivitySubject; // Value injected by FXMLLoader

    @FXML
    void saveChanges(ActionEvent event) {

    }

    @FXML // This method is called by the FXMLLoader when initialization is complete
    void initialize() {
        assert btnSaveChanges != null : "fx:id=\"btnSaveChanges\" was not injected: check your FXML file 'Edit_Activity_Subject.fxml'.";
        assert txtActivity != null : "fx:id=\"txtActivity\" was not injected: check your FXML file 'Edit_Activity_Subject.fxml'.";
        assert cmbActivitySubject != null : "fx:id=\"cmbActivitySubject\" was not injected: check your FXML file 'Edit_Activity_Subject.fxml'.";

    }
}
