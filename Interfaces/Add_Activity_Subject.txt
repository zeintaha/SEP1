/**
 * Sample Skeleton for 'Add_Activity_Subject.fxml' Controller Class
 */

import java.net.URL;
import java.util.ResourceBundle;
import javafx.fxml.FXML;
import javafx.scene.control.Button;
import javafx.scene.control.TextField;

public class PleaseProvideControllerClassName {

    @FXML // ResourceBundle that was given to the FXMLLoader
    private ResourceBundle resources;

    @FXML // URL location of the FXML file that was given to the FXMLLoader
    private URL location;

    @FXML // fx:id="btnAddActivitySubject"
    private Button btnAddActivitySubject; // Value injected by FXMLLoader

    @FXML // fx:id="txtActivitySubject"
    private TextField txtActivitySubject; // Value injected by FXMLLoader

    @FXML
    void addActivitySubject(ActionEvent event) {

    }

    @FXML // This method is called by the FXMLLoader when initialization is complete
    void initialize() {
        assert btnAddActivitySubject != null : "fx:id=\"btnAddActivitySubject\" was not injected: check your FXML file 'Add_Activity_Subject.fxml'.";
        assert txtActivitySubject != null : "fx:id=\"txtActivitySubject\" was not injected: check your FXML file 'Add_Activity_Subject.fxml'.";

    }
}
